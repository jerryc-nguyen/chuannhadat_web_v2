// Main component export
export { default } from './InfoPanel';

// Export types for external usage
export type { InfoPanelProps, ProfileDataState, InfoPanelSection, UseInfoPanelOptions } from './types';

// Export hooks for external usage
export { useInfoPanelData } from './hooks';

// Export components for external usage if needed
export * from './components';
